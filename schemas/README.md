# Schemas

This folder contains history of database schemas.

- `schema_v1.drawio` — initial reviewed schema version.
- `schema_v2.drawio` — implemented variant B for `client_id` as `STRING` and aligned loading/partition/key metadata.